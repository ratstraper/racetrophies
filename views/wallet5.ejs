<html>
<head>
    <!--  Rainbow пока нет -->
      <!-- + держит подключение кошелька -->
</head>
<body>
    <div><w3m-button></w3m-button></div>
    <!-- <button type="button" data-testid="rk-connect-button">Connect Wallet</button> -->
    <div id="address"></div>
    <div id="btn"></div>
    <div id="info">Rainbow + wagmi</div>
    <script type="module">
        // const connect_button = document.createElement("ConnectButton");
        // document.getElementById("btn").outerHTML = connect_button.outerHTML;
        import { getDefaultConfig } from "https://esm.sh/@rainbow-me/rainbowkit@2.1.6";
        import { coinbaseWallet, trustWallet, braveWallet } from "https://esm.sh/@rainbow-me/rainbowkit@2.1.6/wallets";
        import { polygon } from "https://esm.sh/@wagmi/chains@1.8.0";
        import { reconnect, disconnect, getAccount, getEnsName, switchChain } from "https://esm.sh/@wagmi/core@2.13.5?exports=reconnect,disconnect,getAccount,getEnsName,switchChain"
        import { createWeb3Modal } from "https://esm.sh/@web3modal/wagmi@5.1.9/?bundle"
        

        // Enable Coinbase Smart Wallet for testing
        coinbaseWallet.preference = "smartWalletOnly";

        export const config = getDefaultConfig({
            appName: 'RT_Auth',
            projectId: '64fdce6d87f6cfcc448d686b57d8d096',
            chains: [polygon],
            // wallets: [
            //     {
            //     groupName: "Recommended",
            //     wallets: [coinbaseWallet, trustWallet, metaMaskWallet, rainbowWallet],
            //     },
            // ],
            // ssr: true,
        });

        reconnect(config);

        const modal = createWeb3Modal({
            wagmiConfig: config,
            projectId: '64fdce6d87f6cfcc448d686b57d8d096',
            // chains: chains,             // +
            // defaultChain: polygon       // +
        });

      </script>
</body>
</html>